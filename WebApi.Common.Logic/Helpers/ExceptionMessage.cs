using System;

namespace WebApi.Common.Logic.Helpers
{
    public static class ExceptionMessage
    {
        public static void Show(Exception ex)
        {
            //if (ex is ArgumentException)
            //    MessageBox.Show(Exceptions.ResourceManager.GetString(ConfigStrings.ArgumentException));
            //if (ex is ArgumentNullException)
            //    MessageBox.Show(Exceptions.ResourceManager.GetString(ConfigStrings.ArgumentNullException));
            //if (ex is ArgumentOutOfRangeException)
            //    MessageBox.Show(Exceptions.ResourceManager.GetString(ConfigStrings.ArgumentOutOfRangeException));
            //if (ex is DirectoryNotFoundException)
            //    MessageBox.Show(Exceptions.ResourceManager.GetString(ConfigStrings.DirectoryNotFoundException));
            //if (ex is FileNotFoundException)
            //    MessageBox.Show(Exceptions.ResourceManager.GetString(ConfigStrings.FileNotFoundException));
            //if (ex is FormatException)
            //    MessageBox.Show(Exceptions.ResourceManager.GetString(ConfigStrings.FormatException));
            //if (ex is IOException)
            //    MessageBox.Show(Exceptions.ResourceManager.GetString(ConfigStrings.IOException));
            //if (ex is OutOfMemoryException)
            //    MessageBox.Show(Exceptions.ResourceManager.GetString(ConfigStrings.OutOfMemoryException));
            //if (ex is OverflowException)
            //    MessageBox.Show(Exceptions.ResourceManager.GetString(ConfigStrings.OverflowException));
            //if (ex is PlatformNotSupportedException)
            //    MessageBox.Show(Exceptions.ResourceManager.GetString(ConfigStrings.PlatformNotSupportedException));
            //if (ex is TargetException)
            //    MessageBox.Show(Exceptions.ResourceManager.GetString(ConfigStrings.TargetException));
        }
    }
}
